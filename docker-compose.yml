version: '3.8'

services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      # Note: Make sure to map your environment variables here
      # You can also use env_file: .env
    env_file:
      - .env
    restart: always
